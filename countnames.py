"""Program to enter 10 names and count only those which are greater than 5 letters """
from array import *

x = array[]
	for i in range(0,10):
		x = int(input("Enter the ten names"))
		count = 0
	for i in x:
		if len(x)>5:
			count+ = 1
print count	
